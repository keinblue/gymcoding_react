services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ../:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
    # depends_on:
    #   - db

#   db:
#     image: postgres:15
#     environment:
#       - POSTGRES_DB=gymcoding
#       - POSTGRES_USER=postgres
#       - POSTGRES_PASSWORD=password
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data: